server:
  host: "0.0.0.0"
  port: 8000
  reload: true

monitoring:
  drift_threshold: 0.05
  performance_check_interval: 3600

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"